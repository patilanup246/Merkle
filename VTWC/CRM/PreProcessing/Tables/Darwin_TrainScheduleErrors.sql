CREATE TABLE [PreProcessing].[Darwin_TrainScheduleErrors] (
    [JobID]     VARCHAR (512) NULL,
    [Username]  VARCHAR (512) NULL,
    [Timestamp] VARCHAR (512) NULL,
    [Record]    VARCHAR (512) NULL,
    [Reason]    VARCHAR (512) NULL
);

